# ============================================================
# Sao chép file này thành .env.local để chạy ở máy.
# Trên Vercel: khai báo trong Project Settings → Environment Variables.
# TUYỆT ĐỐI không commit giá trị thật.
# ============================================================

# --- Client (CRA nhúng vào bundle lúc build → CHỈ đặt khoá công khai) ---
REACT_APP_SUPABASE_URL=https://wlcaedkbrbaiqqjyxyph.supabase.co
REACT_APP_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxx

# --- Server (chỉ Vercel Function đọc được, không vào bundle) ---
SUPABASE_URL=https://wlcaedkbrbaiqqjyxyph.supabase.co
SUPABASE_SECRET_KEY=sb_secret_xxxxxxxxxxxxxxxxxxxx

# --- Chỉ dùng cho script nhập dữ liệu cũ (scripts/import-sheet.mjs) ---
# SUPABASE_SECRET_KEY ở trên được tái sử dụng.
